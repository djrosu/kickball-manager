spring:
  application:
    name: singles-sports-social

server:
  port: ${PORT:8080}

app:
  base-url: ${APP_BASE_URL:http://localhost:8080}
  mail:
    from: ${APP_MAIL_FROM:no-reply@localhost}
  uploads:
    root-path: ${APP_UPLOADS_ROOT_PATH:./uploads}